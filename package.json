{
  "name": "visualizer-studio",
  "version": "1.0.0",
  "description": "Local music visualizer app that turns audio and artwork into MP4 videos.",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "build": "apt-get update && apt-get install -y python3 python3-pip ffmpeg && pip3 install Pillow",
    "railway:start": "node server.js"
  },
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "ffprobe-static": "^3.1.0"
  }
}
